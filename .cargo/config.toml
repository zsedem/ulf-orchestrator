# Use gold linker to avoid OOM on low-memory systems (~2GB RAM).
# The default ld linker gets killed when linking the ~300MB debug ulf-cli binary.
[target.aarch64-unknown-linux-gnu]
linker = "gcc"
rustflags = ["-C", "link-arg=-fuse-ld=gold", "-C", "debuginfo=0"]
