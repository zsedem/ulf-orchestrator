version: '3.8'

services:
  # Main Ulf Orchestrator service
  ulf:
    build:
      context: .
      dockerfile: Dockerfile
    image: ulf-orchestrator:local
    container_name: ulf-orchestrator
    environment:
      # AI Agent API Keys (set in .env file)
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - Q_API_KEY=${Q_API_KEY}
      
      # Ulf Configuration
      - ULF_AGENT=${ULF_AGENT:-auto}
      - ULF_MAX_ITERATIONS=${ULF_MAX_ITERATIONS:-100}
      - ULF_MAX_RUNTIME=${ULF_MAX_RUNTIME:-14400}
      - ULF_MAX_TOKENS=${ULF_MAX_TOKENS:-1000000}
      - ULF_MAX_COST=${ULF_MAX_COST:-50.0}
      - ULF_CHECKPOINT_INTERVAL=${ULF_CHECKPOINT_INTERVAL:-5}
      - ULF_VERBOSE=${ULF_VERBOSE:-false}
      - ULF_ENABLE_METRICS=${ULF_ENABLE_METRICS:-true}
      
      # Logging
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      # Mount workspace for task execution
      - ./workspace:/workspace
      # Mount prompts directory (read-only)
      - ./prompts:/prompts:ro
      # Mount agent state directory
      - ulf-agent:/app/.agent
      # Mount cache directory
      - ulf-cache:/app/.cache
      # Mount git directory if needed
      - ./.git:/workspace/.git
    networks:
      - ulf-network
    restart: unless-stopped
    command: 
      - "--agent=${ULF_AGENT:-auto}"
      - "--prompt=/workspace/PROMPT.md"
      - "--verbose"
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Redis for caching and state management
  redis:
    image: redis:7-alpine
    container_name: ulf-redis
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - ulf-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL for persistent storage (optional)
  postgres:
    image: postgres:15-alpine
    container_name: ulf-postgres
    environment:
      - POSTGRES_DB=ulf_orchestrator
      - POSTGRES_USER=ulf
      - POSTGRES_PASSWORD=${DB_PASSWORD:-ulf_secret}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - ulf-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ulf"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles:
      - with-db

  # Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: ulf-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - ulf-network
    ports:
      - "9090:9090"
    restart: unless-stopped
    profiles:
      - monitoring

  # Grafana for visualization
  grafana:
    image: grafana/grafana:latest
    container_name: ulf-grafana
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin123}
      - GF_INSTALL_PLUGINS=redis-datasource
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    networks:
      - ulf-network
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      - prometheus
    profiles:
      - monitoring

  # Documentation server (development)
  docs:
    image: squidfunk/mkdocs-material:latest
    container_name: ulf-docs
    volumes:
      - .:/docs
    networks:
      - ulf-network
    ports:
      - "8000:8000"
    command: serve --dev-addr=0.0.0.0:8000
    profiles:
      - development

# Volumes for persistent data
volumes:
  ulf-agent:
    driver: local
  ulf-cache:
    driver: local
  redis-data:
    driver: local
  postgres-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

# Networks
networks:
  ulf-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16